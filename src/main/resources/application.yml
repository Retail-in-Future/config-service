spring:
  application:
   name: ratail-config
  cloud:
    config:
      server:
        git:
          uri: http://10.207.11.201:8000/git/config_service
          searchPaths: config-repo
        server:
          monitor:
            github:
              enabled: true
      bus: enabled


  rabbitmq:
    port: 5672
    host: '10.207.11.202'
    username: guest
    password: guest
    listener:
      max-concurrency: 1
      prefetch: 1
      acknowledge-mode: auto
      auto-startup: true
    dynamic: true

eureka:
      instance:
        instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}

server:
  port: 8888
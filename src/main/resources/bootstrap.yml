spring:
  application:
   name: ratail-config
  cloud:
    bus:
      enabled: true
    config:
      server:
        git:
          uri: http://10.207.11.201:8000/git/config_service
          searchPaths: config-repo
        server:
          monitor:
            github:
              enabled: true


  rabbitmq:
    port: 5672
    host: '10.207.11.202'
    username: guest
    password: guest
    listener:
      acknowledge-mode: auto
      auto-startup: true
    dynamic: true

server:
  port: 8888